info:
  name: Post-Login
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  body:
    type: json
    data: |-
      {
        "email": "teste@email.com",
        "password": "123456",
        "cpf": "12345678909"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
